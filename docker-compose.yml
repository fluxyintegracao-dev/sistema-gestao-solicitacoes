version: '3'
services:
  db:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: solicitacoes
    ports:
      - "0.0.0.0:3306:3306"
    networks:
      - default
