# Logs de Desenvolvimento - Changelog

Este arquivo registra mudancas relevantes de produto, arquitetura e documentacao.

## 2026-04 (continuacao - sesao 12/04)

- correcao do fluxo CSRF em ambiente com frontend e API em subdominios distintos, expondo `X-CSRF-Token` via CORS e mantendo o token em memoria no frontend para `POST/PATCH/DELETE` autenticados por cookie, sem expor o JWT HTTP-only
- alinhamento da biblioteca de arquivos modelos com a regra documentada de visualizacao ampla: qualquer usuario autenticado pode listar e abrir arquivos de paginas ativas; upload permanece restrito por pagina/perfil

- reescrita completa de `docs/arquitetura/deploy_ambientes.md`, transformando o antigo guia resumido de deploy em guia mestre de:
  - implantacao tecnica por instalacao
  - configuracao inicial da empresa cliente
  - governanca de modulos e permissoes
  - operacao detalhada por modulo e tela
  - roteiro de treinamento por perfil
  - checklist de prints para apresentacoes
  - suporte interno do administrador e suporte externo do provedor
- atualizacao de `docs/README.md` para promover `docs/arquitetura/deploy_ambientes.md` como referencia operacional principal da implantacao
- criacao dos artefatos derivados do guia mestre:
  - `docs/ROTEIRO_APRESENTACAO_TREINAMENTO_FLUXY.md` com ordem de demonstracao, script de apresentacao, sequencia de slides e lista de prints
  - `docs/CHECKLIST_IMPLANTACAO_CLIENTE_FLUXY.md` com checklist preenchivel por cliente cobrindo deploy, configuracao, treinamento, go-live e aceite
- atualizacao de `docs/README.md` para incluir os novos documentos operacionais de implantacao e treinamento

- reforco da governanca modular nas telas de configuracao, com:
  - nova matriz operacional em `Configuracoes > Modulos e Planos` para explicar o efeito real de `SOLICITACOES`, `CONTRATOS`, `OBRAS` e `COMPRAS`
  - indicacao visual do status do modulo dentro de `Configuracoes > Permissoes de Areas por Usuario`
  - aviso explicito de que permissoes de area em modulo desligado podem ficar preconfiguradas, mas nao entram em vigor ate a ativacao
  - separacao da linguagem por perfil em `Permissoes de Areas por Usuario`, mantendo leitura estrutural para `SUPERADMIN` e orientacao operacional simplificada para `ADMINISTRADOR`

- ajuste da modularidade em `Nova Solicitacao` e na listagem de `Solicitacoes`, com:
  - manutencao do parceiro selecionado dentro do proprio campo de busca, sem card redundante abaixo
  - degradacao segura da tela quando `CONTRATOS` ou `OBRAS` estiverem desabilitados na instalacao
  - ocultacao e nao obrigatoriedade dos campos dependentes desses modulos, sem remover colunas do banco
  - carga isolada de contratos e apropriacoes por obra, sem derrubar um bloco quando o outro modulo nao estiver disponivel
  - ocultacao das colunas `Contrato` e `Ref. do Contrato` na tela de `Solicitacoes` quando o modulo `CONTRATOS` nao estiver habilitado
  - exportacao CSV de `Solicitacoes` alinhada a mesma regra de visibilidade do modulo `CONTRATOS`
  - detalhe da solicitacao alinhado a disponibilidade de `CONTRATOS` e `OBRAS`
  - protecao backend da rota de atualizacao de ref. de contrato quando `CONTRATOS` estiver desabilitado
- desacoplamento tecnico do cadastro de apropriacoes, com:
  - novo dominio compartilhado em `GET/POST/PUT/DELETE /apropriacoes`
  - middleware backend para liberar leitura quando ao menos um modulo consumidor estiver habilitado
  - gestao administrativa de apropriacoes reposicionada para o contexto de `OBRAS`
  - `Nova Solicitacao` e `Financeiro > Novo Titulo` passando a tratar apropriacao como dependencia de `OBRAS`
  - servico frontend compartilhado `services/apropriacoes.js`
  - documentacao de implantacao e matriz modular registrada em `docs/arquitetura/modularidade_solicitacoes_contratos_apropriacoes.md`

- incorporacao do modulo `PROVISOES` do backup para o `fluxy-core`, com:
  - habilitacao por instalacao no mesmo padrao dos demais modulos
  - entrada na matriz de permissoes de areas por usuario em `Configuracoes`
  - backend dedicado com modelos, service, controllers, validators e rotas em `/provisoes-financeiras`
  - telas `Dashboard de Previsao`, `Provisionamentos`, `Nova Provisao`, `Detalhe da Provisao` e `Categorias Macro`
  - workflow de status `previsto`, `em_analise`, `aprovado`, `cancelado` e `realizado`
  - comentarios, anexos, historico e codigo sequencial por obra
  - manutencao do padrao visual atual do FLUXY no frontend
  - documentacao oficial do modulo em `docs/modulos/provisionamento_financeiro.md`
- refinamento funcional do modulo `PROVISOES`, com:
  - remocao do comentario inicial da tela `Nova Provisao`, concentrando comentarios no detalhe
  - envio de anexos ja na criacao da provisao, com upload automatico apos salvar
  - remocao da UX de workflow de status e da permissao de area `provisoes.status.gerenciar` da configuracao por usuario
  - dashboard simplificado para leitura gerencial de previsao, concentracao, proximidade e curva temporal
  - alinhamento visual das paginas do modulo ao padrao atual de formularios e cards do FLUXY

- classificacao de obras com campos financeiros, com:
  - campo `classificacao` VARCHAR(20): PRIVADA ou PUBLICA
  - campo `vgv` DECIMAL(14,2): Valor Geral de Vendas, exclusivo de obras PRIVADA
  - campo `planilha_geral` DECIMAL(14,2): valor contratado/licitado, exclusivo de obras PUBLICA
  - campo `margem_custo_esperada` DECIMAL(5,2): percentual de custo esperado sobre o valor de referencia
  - orcamento calculado: valor_referencia * (1 - margem / 100)
  - migration `202604120002_obras_classificacao_orcamento.js` aplicada
  - modal de cadastro e edicao de obra atualizado com campos condicionais por classificacao
  - campos de valor formatados como moeda (R$) e margem como percentual (%) nos inputs do modal
  - badge de classificacao exibido nos cards de obras
  - valores de VGV, planilha geral, margem e orcamento exibidos nos cards de obras quando preenchidos
  - controller de obras atualizado para aceitar e validar os novos campos em criar e editar
  - servico `obraGestaoService.js` atualizado para incluir os campos financeiros no retorno do endpoint de gestao

- pagina Resultado de Obras no modulo financeiro, com:
  - rota `GET /financeiro/relatorios/resultado-obras`
  - controller `ResultadoObrasController.js` agrega `TituloFinanceiro` por obra e tipo
  - executado = soma de `valor_baixado` dos titulos PAGAR (exceto cancelados e estornados)
  - recebido = soma de `valor_baixado` dos titulos RECEBER (exceto cancelados e estornados)
  - falta receber = soma de `valor_saldo` dos titulos RECEBER
  - orcamento calculado no backend pelo valor de referencia e margem da obra
  - pagina `FinanceiroResultadoObras.jsx` com cards por obra, barras de progresso e totais consolidados
  - filtro por classificacao: Todas / Privadas / Publicas
  - link adicionado na pagina de Relatorios Financeiros

- sistema de permissoes de areas por usuario (modular, multi-empresa), com:
  - registro central de permissoes em `backend/src/constants/moduloPermissoes.js`
    - 8 modulos cobertos: SOLICITACOES, COMPRAS, FINANCEIRO, OBRAS, CONTRATOS, COMERCIAL, BIBLIOTECA_MODELOS, COMUNICACAO_INTERNA
    - 33 permissoes no formato `modulo.area.acao` (ex: `financeiro.titulos.criar`)
  - armazenamento em `ConfiguracaoSistema` chave `PERMISSOES_AREAS_USUARIOS`
  - mesmo padrao de cache 30s dos demais sistemas de permissao
  - novas funcoes em `authorizationService.js`: `getPermissoesAreasUsuarios`, `getAreasPermissoesForUser`, `invalidatePermissoesAreasCache`
  - sessao do usuario recebe `areas_permissoes: [chave1, chave2, ...]` via `buildSessionUser`
  - helper `hasPermissao(user, 'chave')` adicionado em `acessoProduto.js` com regra:
    - SUPERADMIN/ADMINISTRADOR: bypass total
    - sem configuracao: acesso completo (compatibilidade retroativa)
    - com configuracao: somente chaves listadas valem
  - controller `PermissoesAreasController.js` com endpoints GET, PUT e registry
  - rotas: `GET/PUT /configuracoes/permissoes-areas` e `GET /configuracoes/permissoes-areas/registry`
  - pagina administrativa `PermissoesAreas.jsx` com:
    - painel esquerdo: lista de usuarios com busca e indicador de quantidade configurada
    - painel direito: arvore de modulos > areas > permissoes com checkboxes
    - acoes rapidas de marcar/desmarcar tudo por modulo
    - contadores de permissoes por area e por modulo
    - indicacao de bypass para SUPERADMIN e ADMINISTRADOR
  - link adicionado em Configuracoes: Permissoes de Areas por Usuario

## 2026-04

- implementacao da fundacao do modulo `COMERCIAL` no backend e frontend web
- criacao da base de dados para empreendimentos, unidades, contratos comerciais e parcelas financeiras
- geracao automatica de titulos a receber no financeiro a partir de contratos comerciais
- extensao da baixa financeira para suportar dinheiro, PIX, cartao, transferencia, boleto, cheque, permuta, bens e outros
- extensao do cadastro mestre de parceiros para classificar registros tambem como `CORRETOR`
- ampliacao da tela de contratos comerciais para gerar agendas mensais, trimestrais, semestrais, anuais ou com datas e valores pre-definidos
- integracao de corretor e comissao com o financeiro, com geracao de titulo a pagar para a corretagem no contrato comercial
- refatoracao da tela de contratos comerciais para compor recebiveis por multiplas formas de pagamento ate fechar o valor total do contrato
- mascara monetaria nos campos de valor do contrato comercial e detalhe complementar para `BENS`, `PERMUTA` e `OUTROS` usando as observacoes das parcelas
- separacao explicita da chave `BOLETOS` para futura homologacao bancaria desacoplada do modulo comercial
- criacao da documentacao oficial de modulo em `docs/modulos/comercial.md`
- implementacao da fase 2 do comercial com:
  - tabelas de preco por empreendimento
  - mapa de unidades
  - indicadores financeiros por contrato
  - sincronizacao de status contratual pelo financeiro
  - distrato guiado com bloqueio por baixas existentes
  - troca de unidade com ajuste automatico nos recebiveis
  - trilha auditavel de eventos comerciais
- criacao da camada intermediaria de cobranca externa no financeiro:
  - dados do boleto emitido no banco vinculados ao titulo existente
  - atualizacao de forma/status de cobranca, banco, nosso numero, linha digitavel e codigo de barras
  - exposicao desses dados na criacao manual, lista e detalhe do titulo
  - preparo do fluxo comercial sem exigir submodulo `BOLETOS` nesta fase
- consolidacao do plano oficial do modulo `RH_DP` e do modulo `INTEGRACAO_SIENGE`, com:
  - habilitacao por instalacao alinhada ao `fluxy_ops`
  - separacao entre RH/DP, Financeiro e gateway tecnico do SIENGE
  - execucao por blocos para reduzir risco de regressao
  - recomendacao de matriz granular de permissoes para RH e contabilidade
- criacao da referencia documental inicial do modulo planejado em `docs/modulos/rh_dp.md`
- implementacao do bloco 1 da fundacao modular de `RH_DP` e `INTEGRACAO_SIENGE`, com:
  - inclusao das chaves no catalogo central de modulos
  - protecao de prefixos de rota no backend
  - helpers de acesso no frontend
  - menu condicional e paginas placeholder sem expor funcionalidade incompleta
- implementacao do bloco 2 do `RH_DP`, com:
  - nova dimensao `rh_empresas_grupo` para representar empresas operacionais do grupo dentro da instalacao
  - base de `rh_colaboradores` e `rh_colaborador_pagamentos`
  - CRUD web inicial para empresas do grupo e colaboradores
  - importacao inicial em massa de colaboradores por CSV/Excel
  - liberacao de leitura para administracao e usuarios com acesso financeiro, mantendo escrita restrita a `SUPERADMIN` e `ADMINISTRADOR`
- implementacao do bloco 3 do `RH_DP`, com:
  - base de `rh_documentos_tipos` e `rh_documentos`
  - checklist documental por vinculo com tipos padrao
  - painel geral de documentos RH/DP
  - secao documental no detalhe do colaborador
  - upload no padrao S3/local fallback do produto
  - links assinados para abertura segura
  - substituicao com historico encadeado do documento
- ajuste de UX no documental do `RH_DP`, com:
  - aviso explicito no cadastro de colaborador de que anexos so aparecem apos o primeiro salvamento
  - acao de upload renomeada para `Anexar arquivo` no detalhe do colaborador
  - novo formulario de envio direto na pagina geral de documentos com busca e selecao do colaborador
- implementacao do bloco 4 do `RH_DP`, com:
  - base de `rh_importacoes` e `rh_importacao_linhas`
  - suporte a importacoes de jornada, evento variavel e desconto
  - preview persistido com validacao por linha
  - confirmacao explicita do lote antes da apuracao
  - tela web de importacoes com modelos CSV, lista e detalhe do preview
- implementacao do bloco 5 do `RH_DP`, com:
  - base de `rh_apuracoes` e `rh_apuracao_eventos`
  - geracao de apuracao por competencia usando apenas lotes `CONFIRMADA`
  - consolidacao por colaborador de jornada, evento variavel e desconto
  - separacao de regra de calculo entre `CLT` e `NAO_CLT`
  - tela web de pre-folha com filtros, detalhe e ajustes manuais por item
  - conferencia da apuracao com bloqueio enquanto existirem itens pendentes
- implementacao do bloco 6 do `RH_DP`, com:
  - base de `rh_fechamentos` e `rh_fechamento_titulos`
  - fechamento da competencia com geracao central de titulos `PAGAR` no modulo `FINANCEIRO`
  - sincronizacao do favorecido do colaborador com `parceiros` para manter a camada financeira aderente ao produto atual
  - tela web de fechamentos RH/DP e formulario de fechamento dentro da pagina de apuracao
  - dependencia explicita do modulo `FINANCEIRO` para o fechamento, sem criar um financeiro paralelo ao RH/DP
- correcao do fluxo de conciliacao bancaria apos criar titulo e registrar baixa manual:
  - a resposta da baixa agora expoe `movimento_financeiro_id` e `movimento`
  - a tela de conciliacao deixou de usar `id` do titulo como fallback para conciliacao
  - mensagens de erro ficaram mais claras quando o movimento selecionado nao estiver mais disponivel
- implementacao da fundacao do bloco 7 da `INTEGRACAO_SIENGE`, com:
  - base `sienge_integracao_config`
  - base `sienge_integracao_fila`
  - base `sienge_integracao_logs`
  - base `sienge_integracao_mapeamentos`
  - tela web de configuracao, prontidao, fila e logs
  - fila persistida por `TituloFinanceiro` central
  - reprocessamento manual da fila
  - preparo para uso de credenciais via `backend/.env`
- implementacao do bloco 8 da `INTEGRACAO_SIENGE`, com:
  - exposicao do status da fila nos retornos do `FINANCEIRO`
  - exposicao do status da fila no detalhe dos fechamentos `RH_DP`
  - acao direta de enviar e reprocessar no detalhe do titulo financeiro
  - acao direta de enviar e reprocessar no detalhe do fechamento RH/DP
  - eliminacao da necessidade de informar `titulo_financeiro_id` manualmente para operar o gateway nas telas consumidoras
- implementacao da matriz granular de permissoes para `RH_DP` e `INTEGRACAO_SIENGE`, com:
  - configuracao por usuario em `ConfiguracaoSistema`
  - sessao autenticada passando capacidades granulares ao frontend
  - backend validando capacidade por acao nas rotas de colaboradores, documentos, importacoes, apuracao, fechamentos e gateway SIENGE
  - nova tela administrativa `Permissoes RH/DP e SIENGE por usuario`
  - separacao entre leitura e operacao para equipe de RH e contabilidade, sem criar perfil hardcoded novo
- ajuste da configuracao tecnica do gateway `INTEGRACAO_SIENGE`, com:
  - suporte a base pronta por `SIENGE_API_BASE_URL`
  - suporte a base composta por `SIENGE_API_HOST` + `SIENGE_API_SUBDOMAIN` + `SIENGE_API_BASE_PATH`
  - suporte a endpoint padrao de titulos por `SIENGE_ENDPOINT_TITULOS`
  - reserva de `SIENGE_ENDPOINT_CREDORES` para a futura fase de cadastro de credor
  - exposicao da origem efetiva da base URL e do endpoint na tela da integracao
- consolidacao da referencia tecnica dos endpoints de credor do SIENGE, com:
  - reserva de `SIENGE_ENDPOINT_CREDOR_DETALHE`
  - reserva de `SIENGE_ENDPOINT_CREDOR_BANK_INFORMATIONS`
  - reserva de `SIENGE_ENDPOINT_CREDOR_PIX_INFORMATIONS`
  - formalizacao de `creditorId` como identificador externo variavel do SIENGE dentro do desenho do FLUXY
- fundacao tecnica da fase de credores no gateway `INTEGRACAO_SIENGE`, com:
  - contexto por parceiro para avaliar prontidao e catalogo efetivo de endpoints
  - vinculacao manual de `external_creditor_id` por parceiro
  - sincronizacao do `external_creditor_id` conhecido na fila e no payload do titulo
- ampliacao operacional da fase de credores no gateway `INTEGRACAO_SIENGE`, com:
  - busca de credor no SIENGE via `GET /creditors` com paginacao controlada
  - matching exato por documento e nome para reduzir vinculos indevidos
  - vinculo automatico somente quando houver match exato unico
  - tela inicial para buscar parceiro interno, consultar candidatos e gravar `creditorId`
- consolidacao da politica opcional de automacao de credor no gateway `INTEGRACAO_SIENGE`, com:
  - novas flags persistidas de instalacao para auto vinculacao por busca exata e auto cadastro futuro
  - padrao conservador desligado por default
  - tentativa automatica de vincular `creditorId` existente por busca exata apenas quando a instalacao habilitar esse comportamento
  - separacao explicita entre auto vinculacao segura e cadastro automatico futuro via `POST /creditors`
- evolucao da fase de credores no gateway `INTEGRACAO_SIENGE`, com:
  - `POST /creditors` manual a partir do parceiro interno
  - tentativa automatica opcional de cadastro de credor antes do envio do titulo
  - template configuravel em `payload_defaults` para acomodar o schema efetivo do ambiente SIENGE
  - validacao minima local do payload antes de enviar ao endpoint externo
  - persistencia do `external_creditor_id` apos cadastro bem-sucedido
- ajuste de UX no documental do `RH_DP`, com:
  - remocao do envio de documentos na pagina geral `Documentos`
  - concentracao do anexo apenas na pagina de `Colaboradores`
  - manutencao da pagina geral como painel de consulta, filtros, abertura e substituicao
- correcao do fluxo de criacao manual na conciliacao bancaria, com:
  - endpoint transacional para criar titulo, registrar baixa e confirmar conciliacao em uma unica operacao
  - eliminacao do fluxo fragil de tres chamadas independentes no modal de conciliacao
  - bloqueio extra contra reenvio no submit do modal
  - validacao obrigatoria de tipo, conta e valor contra o proprio lancamento OFX antes de gravar qualquer dado
- planejamento documental do novo modulo comercial para construcao civil, com separacao entre `COMERCIAL` e `BOLETOS`
- atualizacao da documentacao central para refletir a direcao do FLUXY como ERP simples, intuitivo, de facil registro e controle
- consolidacao da recomendacao arquitetural de manter o financeiro como motor central de titulos e movimentos para modulos futuros
- auditoria ampla da documentacao com base no codigo atual de `backend/src`, `frontend/src` e `docs/`
- atualizacao do `README.md` principal para refletir:
  - migrations controladas
  - deploy atual
  - modulos ativos
  - stack real
- atualizacao da trilha documental em `docs/contexto`, `docs/arquitetura`, `docs/modulos`, `docs/regras_negocio` e `docs/seguranca`
- consolidacao do entendimento atual sobre:
  - contas manuais no financeiro
  - cotacao com parceiros por categoria
  - pedidos de compra derivados da cotacao
  - gestao de obras com parcelas e custos
- correcao visual do dashboard de `PROVISOES`, com:
  - restauracao da base de layout `page dashboard`
  - inclusao das classes visuais faltantes para KPI, paineis, barras e alertas no `frontend/src/index.css`
  - confirmacao de que o problema era de CSS no frontend, nao de migration
- correcao das regras de acesso do modulo `CONTRATOS` para `ADMINISTRADOR`, com:
  - remocao do check local antigo na tela `GestaoContratos`, passando a usar o helper central `canAccessContratos`
  - alinhamento dos middlewares de recurso e acesso a anexos para tratar `ADMINISTRADOR` como acesso global valido do modulo
  - eliminacao da divergencia entre configuracao de permissoes, menu e fluxos operacionais de contrato
- padronizacao da documentacao em ASCII para reduzir risco de erro de encoding

## 2026-03

- cadastro mestre de parceiros
- apropriacao por item em compras
- financeiro com titulos, baixa, estorno e conciliacao OFX
- dashboards e relatorios de fluxo de caixa
- camada de seguranca com JWT, rate limit, validacoes e auditoria
- gestao de obras com orcamento por apropriacao
- multa adicionada nos movimentos financeiros
